# OWASP ZAP baseline

Requiere Docker y auth-service corriendo en localhost:8180.

```bash
chmod +x tests/security/zap-baseline.sh
./tests/security/zap-baseline.sh
```

Variable `TARGET` para otro host.
