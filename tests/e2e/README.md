# Pruebas E2E

Smoke test de flujo básico: health → login → analytics.

```bash
chmod +x tests/e2e/smoke.sh
./tests/e2e/smoke.sh
```

Variables opcionales: `AUTH_URL`, `GATEWAY_URL`, `DASHBOARD_URL`.
